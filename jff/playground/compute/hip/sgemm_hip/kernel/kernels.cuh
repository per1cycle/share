#ifndef KERNELS_CUH
#define KERNELS_CUH

template <typename T>
void run_kernel(int kernel_type, T *a, T *b, T *c, int M, int N, int K, float alpha, float beta)
{
    switch(kernel_type)
    {
        case 0:
        {
            break;
        }
        case 1:
        {
            
            break;
        }
        
    }
}

#endif